---
/**
 * Site-specific Footer wrapper.
 * Delegates to the shared design-system Footer with local links (since this
 * IS the aegis-initiative site — no need to point at external URLs).
 */
import SharedFooter from '@aegis-initiative/design-system/components/Footer.astro';
---

<SharedFooter localLinks={true} />
