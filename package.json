{
  "name": "aegis-initiative",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && pagefind --site dist",
    "postbuild": "cp -r dist/pagefind public/",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^5.0.2",
    "@pagefind/default-ui": "^1.4.0",
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "pagefind": "^1.4.0"
  }
}
